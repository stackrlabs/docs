# Off-chain Data [Local storage]

Every micro-rollop maintains a local copy of the data. This data is stored in a local database. Stackr allows micro-rollups to plug in most major database systems like `sqlite`, `postgres` etc
